{{- if .Values.podMonitor.enabled }}
apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: seagate-csi-node-exporter
  labels:
{{ include "csidriver.labels" . | indent 4 }}
spec:
  selector:
    matchLabels:
      name: seagate-csi-node-server
  podMetricsEndpoints:
  - port: metrics
{{- end }}
